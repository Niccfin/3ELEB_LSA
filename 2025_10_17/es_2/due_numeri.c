#include<stdio.h>
#include<stdlib.h>

int main(){
    // viene definita la prima variabile intera
    int variabile1 = 3;
    //viene definita la seconda variabile intera
    int variabile2 = 2;
    //attraverso il comando printf vengono stampati i due numeri e infine vengono stampati da sommati
    printf("Primo numero: %d, Secondo numero: %d, Somma dei due numeri: %d", variabile1, variabile2, variabile1 + variabile2);
    return 0;
}
